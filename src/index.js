import validator from "./validator.js";
console.log(validator);

const intro = document.getElementById("introView");
const footer = document.getElementById("footer");

const introViewChange = (event) => {
  console.log(event);
};
